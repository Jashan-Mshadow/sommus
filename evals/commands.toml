# The 20 Commands — real things Jashan would type. Edit freely: these define
# what Phase 1 must handle, and become the eval (target: 18/20 pass).
#
# expect = tools that must be called (in any order). [] means no tool should run.
# confirm = true if the permission gate should ask before running.

[[command]]
text = "what's my battery at"
expect = ["get_battery"]

[[command]]
text = "volume 30"
expect = ["set_volume"]

[[command]]
text = "mute"
expect = ["set_mute"]

[[command]]
text = "turn it up a bit"
expect = ["get_volume", "set_volume"]

[[command]]
text = "what apps do I have open"
expect = ["list_apps"]

[[command]]
text = "open spotify"
expect = ["open_app"]

[[command]]
text = "pause the music"
expect = ["media_control"]

[[command]]
text = "skip this song"
expect = ["media_control"]

[[command]]
text = "open my ECE 150 LEARN page"
expect = ["open_url"]

[[command]]
text = "pull up the MATH 115 textbook on google"
expect = ["open_url"]

[[command]]
text = "quit messages"
expect = ["quit_app"]
confirm = true

[[command]]
text = "close everything except chrome and obsidian"
expect = ["list_apps", "quit_app"]
confirm = true

[[command]]
text = "remind me in a notification to drink water"
expect = ["notify"]

[[command]]
text = "lock my laptop"
expect = ["lock_screen"]

[[command]]
text = "turn off the screen"
expect = ["sleep_display"]

[[command]]
text = "put the laptop to sleep"
expect = ["sleep_computer"]
confirm = true

[[command]]
text = "study mode: close messages, open obsidian, volume 10"
expect = ["quit_app", "open_app", "set_volume"]
confirm = true

[[command]]
text = "is it loud right now"
expect = ["get_volume"]

# --- added 2026-09-14 with the second batch of tools

[[command]]
text = "set my brightness to 40"
expect = ["set_brightness"]

[[command]]
text = "screen's too dim"
expect = ["set_brightness"]

[[command]]
text = "what wifi am I on"
expect = ["get_wifi"]

[[command]]
text = "what's playing right now"
expect = ["get_now_playing"]

[[command]]
text = "what shortcuts do I have"
expect = ["list_shortcuts"]

[[command]]
text = "remind me to email WARG tonight at 8"
expect = ["create_reminder"]

[[command]]
text = "what's on my clipboard"
expect = ["get_clipboard"]

[[command]]
text = "find my resume tex file"
expect = ["find_files"]

[[command]]
text = "press escape for me"
expect = ["press_keys"]

# --- answering, not acting: it should look things up instead of refusing

[[command]]
text = "what's the weather in Waterloo right now"
expect = ["web_search"]

[[command]]
text = "explain what a semaphore is in one sentence"
expect = []

[[command]]
text = "what's 17% of 340"
expect = []

# No tool for this yet. Sommus should improvise (open the Bluetooth settings pane)
# or say what's missing — both are fine, so no expectation is set.
[[command]]
text = "connect my bluetooth speaker"
